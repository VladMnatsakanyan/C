#include <stdio.h>
#include <string.h>

int main(void)
{
    char word[20];
    int count = 0;
    scanf("%s", word);
    while(word[count] != '\0') {
        count++;
    }
    printf("%d", count);
    return 0;
}
