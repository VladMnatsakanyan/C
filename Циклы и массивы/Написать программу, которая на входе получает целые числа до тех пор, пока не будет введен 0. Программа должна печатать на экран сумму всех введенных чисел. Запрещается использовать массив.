#include <stdio.h>

int main(void)
{
    int a;
    int sum = 0;
    for(;;) {
        scanf("%d", &a);
        if(a==0) {
            break;
        }
        sum = sum + a;
    }
    printf("%d", sum);
    return 0;
}
