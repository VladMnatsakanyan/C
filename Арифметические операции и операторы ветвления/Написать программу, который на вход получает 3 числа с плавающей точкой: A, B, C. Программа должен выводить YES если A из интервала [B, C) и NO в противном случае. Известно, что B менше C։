#include <stdio.h>

int main(void)
{
    int a;
    int b;
    int c;
    scanf("%d", &a);
    scanf("%d", &b);
    scanf("%d", &c);
    if((a>=b) && (a<c)) {
        printf("YES");
    } 
    else {
        printf("NO");
    }

    return 0;
}
