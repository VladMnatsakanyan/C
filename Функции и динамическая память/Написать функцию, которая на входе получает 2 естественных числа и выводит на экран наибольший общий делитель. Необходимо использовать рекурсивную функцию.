#include <stdio.h>

int bajanarar(int a, int b) {
    if(b == 0) {
        return a;
    } return bajanarar(b, a % b);
}
int main(void)
{
    int x;
    int y;
    scanf("%d", &x);
    scanf("%d", &y);
    int ardyunq = bajanarar(x, y);
    printf("%d", ardyunq);
    return 0;
}
